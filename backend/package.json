{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "sls deploy && aws lambda invoke --function-name api --payload '{}' output.json",
    "docker": "docker run --rm -it -p 4566:4566 -p 4571:4571 localstack/localstack"
  },
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "serverless-localstack": "^1.2.0"
  }
}
